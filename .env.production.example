# Production API URLs — both use the Nginx API Gateway (HTTPS :443)
# Production API URLs — replace with your ALB DNS name
VITE_AUTH_API_URL=https://<alb-dns-name>/api/v1
VITE_PRODUCTS_API_URL=https://<alb-dns-name>/api/v1
