# Use proxy in dev (default). Set to direct URLs in production builds.
VITE_AUTH_API_URL=/api/auth
VITE_PRODUCTS_API_URL=/api/products

# Production: copy .env.production.example to .env.production, then run:
#   npm run build:prod
# or (Windows):
#   .\scripts\build-production.ps1
